<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!--Google Font-->
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@300;400;500;600;700;900&display=swap"
        rel="stylesheet">
    <!-- Font Awesome-->
    <script src="https://kit.fontawesome.com/d25a039e29.js" crossorigin="anonymous"></script>
    <!-- CSS only -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/home.css" type="text/css">

    <title>Main Page</title>
</head>

<body class="body">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/"><img src="/photo/water.png" alt="" srcset="">JAL</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#blogAppContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="blogAppContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item ">
                    <a class="nav-link" href="/"> <i class="fa fa-home" aria-hidden="true"></i> Home <span
                            class="sr-only"></span></a>
                </li>
                <li class="nav-item ">
                    <a class="nav-link" href="/water"> <i class="fa fa-faucet" aria-hidden="true"></i> Water <span
                            class="sr-only"></span></a>
                </li>
            </ul>
            <form class="form-inline my-2 my-lg-0">
                <ul class="navbar-nav my-2 my-lg-0">
                    <% if(!currentUser){ %>
                    <li>
                        <a class="nav-link" href="/login">
                            <i class="fa fa-sign-in" aria-hidden="true"></i> Authentication
                        </a>
                    </li>
                    <% }else{ %>
                    <li class="nav-right">
                        <a class="nav-link" href="#">Signed In as <%= currentUser.username %></a>
                    </li>
                    <li class="nav-right">
                        <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                    </li>
                    <% } %>
                </ul>
            </form>
        </div>
    </nav>
    <% if(error && error.length > 0){ %>
    <div style="margin: 0;" class="alert alert-danger" role="alert">
        <h5><%= error %></h5>
    </div>
    <%} %>
    <% if(success && success.length > 0){ %>
    <div style="margin: 0;" class="alert alert-success" role="alert">
        <h5><%= success %></h5>
        <%} %>
    </div>